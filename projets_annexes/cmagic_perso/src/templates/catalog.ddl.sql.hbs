{{! Db2 DDL generated from the stable CatalogSpec render model. }}
-- Generated from CatalogSpec with catalog.ddl.sql.hbs. Do not edit.
-- Table for {{entityName}}.
CREATE TABLE {{table}} (
{{#each fields}}
    {{column}} {{sqlType}}{{#if required}} NOT NULL{{/if}}{{#if hasFollowingDefinition}},{{/if}}
{{/each}}
{{#each constraints}}
{{#if isPrimaryKey}}
    PRIMARY KEY ({{#each columns}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}){{#if hasFollowingDefinition}},{{/if}}
{{/if}}
{{#if isUnique}}
    UNIQUE ({{#each columns}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}){{#if hasFollowingDefinition}},{{/if}}
{{/if}}
{{#if isValueCheck}}
    CHECK ({{column}} IN ({{#each enumValues}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}})){{#if hasFollowingDefinition}},{{/if}}
{{/if}}
{{/each}}
);
